# Benchmarks

Benchmark implementations for hypotheses H1–H6.

**Status:** Placeholder — benchmarks pending simulator (Phase 2) and baseline implementations (Phase 3).

**Planned benchmarks:**
- `bench_h1/` — history preservation efficiency (BENCH-H1)
- `bench_h2/` — recovery fidelity (BENCH-H2)
- `bench_h3/` — verification completeness (BENCH-H3)
- `bench_h4/` — authority bypass resistance (BENCH-H4)
- `bench_h5/` — ISA simulation overhead (BENCH-H5)
- `bench_h6/` — AI reasoning explainability (BENCH-H6)

See `research/BENCHMARK_PLAN.md` for methodology.

*John E. Arenz — JGA Enterprises, Mendota, Illinois*
